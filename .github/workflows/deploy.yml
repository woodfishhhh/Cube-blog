name: Deploy Next.js to Ubuntu

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js
        run: npm run build

      # 只打包生产运行必需文件
      - name: Package build files
        run: zip -r deploy.zip .next public content package.json package-lock.json next.config.ts

      # 上传到服务器
      - name: Upload to Ubuntu
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "deploy.zip"
          target: ${{ secrets.DEPLOY_DIR }}

      # 服务器部署（无 Nginx，纯 Next.js）
      - name: Deploy & Restart
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            mkdir -p ${{ secrets.DEPLOY_DIR }}
            cd ${{ secrets.DEPLOY_DIR }}

            # 清理旧产物，避免残留文件影响运行
            rm -rf .next public content package.json package-lock.json next.config.ts

            unzip -o deploy.zip
            rm -f deploy.zip

            # 安装生产依赖
            npm ci --omit=dev

            # PM2 重启/启动 Next.js
            pm2 restart 3dblog || pm2 start npm --name "3dblog" -- start
            pm2 save

            # 权限（Ubuntu 通用）
            chmod -R 755 ${{ secrets.DEPLOY_DIR }}
