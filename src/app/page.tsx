export default function HomePage() {
  return (
    <main className="home-stage">
      <section className="home-stage__copy">
        <p className="home-stage__eyebrow">Persistent scene shell</p>
        <h1 className="home-stage__title">
          Monochrome editorial chrome for the next WOODFISH route states.
        </h1>
        <p className="home-stage__body">
          The root layout now keeps a fixed background host, readable foreground
          structure, and restrained motion tokens in place without shipping the
          real scene engine or navigation logic yet.
        </p>
      </section>

      <aside className="shell-panel">
        <p className="shell-panel__label">Scene mount</p>
        <p className="shell-panel__value">Persistent placeholder ready</p>
        <p className="shell-panel__body">
          Future Three.js work can attach to the shared scene host outside this
          page-specific content and remain mounted across route changes.
        </p>
      </aside>
    </main>
  );
}
